name: emotales_web_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  google_fonts: ^6.1.0
  # For playing local video files
  url_launcher: ^6.1.10
  # Removed video_player_web as it's handled automatically by video_player

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # This line tells your app about the new assets folder
  assets:
    - assets/videos/
